#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @i: The integer value
 *
 * Return: i if i >= 0 or -1 * i if i < 0
 */
int _abs(int i)
{
	if (i < 0)
		return (-n);
	else
		return (n);
}
